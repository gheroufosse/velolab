# infra

Docker Compose stack and nginx reverse proxy config. Wired up starting
[Stage 7](../docs/roadmap.md#stage-7--containerised-and-lan-reachable).

- `nginx/` — reverse proxy config
- `compose/` — Docker Compose files
