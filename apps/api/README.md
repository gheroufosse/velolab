# velolab-api

FastAPI backend for velolab. See repo root README and `docs/` for context.

## Development

```
uv sync
uv run ruff check .
uv run ruff format --check .
uv run ty check
uv run pytest
```
